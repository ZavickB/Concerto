{# templates/project/_idea_card.html.twig #}
<div class="card mb-3">
    <div class="card-body">
        <h5 class="card-title">{{ idea.title }}</h5>
        <p class="card-text">{{ idea.description|slice(0, 100) ~ '...' }}</p>
        <p>
            {% for tag in idea.tags %}
                <span class="badge" style="background-color: {{ tag.color }}; color: #fff;">{{ tag.title }}</span>
            {% endfor %}
        </p>
        <a href="#" class="btn btn-primary btn-sm modalbox-comments"
           data-toggle="modal"
           data-target="#commentsModal"
           data-content-url="{{ path('idea_comments', {'id': idea.id}) }}">
            View Comments
        </a>
    </div>
</div>
